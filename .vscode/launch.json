{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Node: Remote Attach",
            "type": "node",
            "request": "attach",
            "protocol": "inspector",
            "address": "localhost",
            "port": 9229,
            "localRoot": "${workspaceFolder}",
            "remoteRoot": "/usr/app",
            "restart": true
        }
    ]
}
