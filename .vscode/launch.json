{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Node: Attach to Remote",
            "type": "node",
            "request": "attach",
            "protocol": "inspector",
            "restart": true,
            "address": "localhost",
            "port": 9229,
            "remoteRoot": "/usr/app",
            "localRoot": "${workspaceFolder}"
        }
    ]
}
